# OpenGIN Explorer Configuration
# Copy this file to .env.local and update values as needed

# External API URL for the Data Platform
# This URL is used by both the client (for display) and server (for proxying)
NEXT_PUBLIC_EXTERNAL_API_URL=https://your-api-service-url/v1/entities

# Server-side only API URL (optional, falls back to NEXT_PUBLIC_EXTERNAL_API_URL)
# Use this if you need a different URL for server-side requests
# EXTERNAL_API_URL=https://your-internal-api-url/v1/entities
